Steps React

npm create vite@latest

.
react

javascript +swc

npm i
npm run dev
.......................
import styles.css

first - construir toda a aparencia em html e criar os componentes e os fragmentos

onchange every time i change the value 
oninput every time i input something

e.prevent default faz com que nao faça refresh quando clico

pôr sempre uma key no elemento de um array , sempre que declaro algo n vezes para depois
poder escolher que elemento alterando caso alguma alteração tenha sido feita,
usar sempre um id no elemento (crypto.random())

Dicas -> ShortCircuting (Array.lenght() === 0 && "NO Todos") -> caso seja verdadde vai mostrar o resultado do lado 
direito do operando(?)

onChange={e => setNewItem(e.target.value) -> e.target ver mais sobre //e


 display: flex; /* Add display:flex */
  align-items: center; /* Center vertically */
  justify-content: center; /* Center horizontally */
  core to display in center